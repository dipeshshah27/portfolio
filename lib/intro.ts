// Hero entrance choreography waits for the preloader wipe to finish
export const PRELOADER_DURATION = 1.1;
export const PRELOADER_EXIT = 0.7;
export const INTRO_DELAY = PRELOADER_DURATION + PRELOADER_EXIT * 0.6;
